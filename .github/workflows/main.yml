name: CI

on: [push]

jobs:
  
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Copy to remote server
      run:  sshpass -p ${{ secrets.ROOT_PASSWORD }} scp . root@198.143.181.18:/home/website
